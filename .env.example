# Read automatically by `docker compose up` from the repo root (compose only, not the
# backend's own uv/dotenv setup — see backend/.env.example for that).
LLM_PROVIDER=mock
GEMINI_API_KEY=
GEMINI_MODEL=
OPENAI_API_KEY=
OPENAI_MODEL=
DEBUG_MODE=false
